# Напишите программу, которая принимает на вход координаты двух точек
# и находит расстояние между ними в 2D пространстве.

x1_A = float(input('Введите координату точки А по оси Х: '))
y1_A = float(input('Введите координату точки А по оси Y: '))
x2_B = float(input('Введите координату точки B по оси Х: '))
y2_B = float(input('Введите координату точки B по оси Y: '))
import math
distance = round(math.sqrt((x2_B - x1_A)**2 + (y2_B - y1_A)**2), 2)
print('Дистанция между точками A и B =', distance)
