# Напишите программу, которая будет на вход принимать число N и выводить числа от -N до N

N = int(input('Введите число N: '))
num_list = list(range(-N, N+1)) # range - создает диапазон от -N До N+1
print(*num_list)


# N = int(input('Введите число N: '))
# for i in range(-N, N+1))
#   print(i, end=', ')

# N = int(input('Введите число N: '))
# print(*range(-N, N+1),sep=', ') # sep= сепаратор (разделитель) между элементами.

# print(*range(-(N:=int(input('Введите значение N '))), N+1),sep=', ') # := - моржовый оператор, сразу объявляет переменную и ее же возвращает

