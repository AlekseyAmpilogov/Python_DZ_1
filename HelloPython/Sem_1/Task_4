# Напишите программу, которая будет принимать на вход дробь и
# показывать первую цифру дробной части числа

num = abs(float(input('Введите число: '))) # abs - полагаю преобразовывает в положительное
print(int(num*10) % 10) if (int(num*10) % 10) != 0 else 'нет'

# num = float(input('Введите дробное число: '))
# string_num = str(num) 
# print(string_num[string_num.find('.') + 1])) # find - метод возвращает целое число (+1) тоже (выводим элемент под таким то индеском после разделителя '.')

# num = float(input('Введите дробное число: '))
# string_num = str(num) 
# print(string_num.split('.') [1][0])) # split - делит строку на две части разделителем '.' возвращает список и выводим элемент под индеском 0 после разделителя)